#include<math.h>
int main()
{
    //加密输入的四位数数字，将其每位加5，超过10的取个位  
	int a, i, n, m=0;
	scanf("%d", &a);
	for (i = 0; i<4; i++)
	{
		n = a % 10;
		a = a / 10;
		n = n + 5;
		n = n % 10;
	    m += n*pow(10,i);

	}
        m=((m/1000+m/10*10)%1000)+1000*(m%10);
        m=((m/100)%10*10)+((m/10)%10*100)+m%10+1000*(m/1000);
	printf("%4d", m);
} 